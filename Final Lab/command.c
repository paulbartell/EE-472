#include <stdio.h>
#include <string.h>

#include "schedule.h"
#include "tasks.h"

void command (void* taskDataPtr) {
	 CommandData* commandDataPtr = (CommandData*) taskDataPtr;
	 
	 while(1)
	 {
		switch(*(commandDataPtr->commandSelection))
		{
		case 0:
			if()
		case 1:
			
		case 2:
			
		case 3:
			
		case 4:
			
		case 5:
			
	 }
	 
}